// import execQuery from '../dbConnection'

// export default repo = {
// 	getData : (req, res) => {
// 		if (!req) { res.status(404).send("there is no code param") }

// 		const result =  execQuery("select dept_dept as code, dept_name as name from dept_master where dept_code = ?", [code]);

// 	}
// }